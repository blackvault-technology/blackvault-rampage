import { ArrowUpRight, Check, Clock3, ExternalLink, Flag, LockKeyhole, Play, Terminal, Trophy } from "lucide-react";
import { Link, useRoute } from "wouter";
import { courses } from "@/data/catalog";
import { Shell, useProgress } from "@/components/AcademyShell";

export default function Course() {
  const [, params] = useRoute("/course/:courseId");
  const course = courses.find((item) => item.id === params?.courseId) || courses[0];
  const { done } = useProgress();
  const allLessons = course.phases.flatMap((phase) => phase.lessons);
  const completed = allLessons.filter((lesson) => done.includes(`${course.id}:${lesson.id}`)).length;
  const total = allLessons.length;
  const firstOpen = allLessons.find((lesson) => !done.includes(`${course.id}:${lesson.id}`)) || allLessons[0];
  const completeChapters = course.phases.filter((phase) => phase.lessons.every((lesson) => done.includes(`${course.id}:${lesson.id}`))).length;
  const progress = Math.round((completed / Math.max(total, 1)) * 100);

  return <Shell><main className="course-page"><section className="course-hero"><div><p className="eyebrow"><span className="lime-dot" /> {course.eyebrow}</p><h1>{course.title}<br /><em>{course.subtitle}</em></h1><p className="course-lede">{course.description}</p><div className="course-meta"><span><Clock3 size={15} /> {course.time}</span><span><Terminal size={15} /> {course.level}</span><a href={course.sourceUrl} target="_blank" rel="noreferrer"><ExternalLink size={15} /> {course.sourceLabel}</a></div><div className="course-hero-actions"><Link className="primary-cta" href={`/course/${course.id}/lesson/${firstOpen?.id}`}><Play size={15} /> {completed ? "Continue learning" : "Start course"}</Link><Link className="secondary-cta" href={`/course/${course.id}/assessment`}><Trophy size={15} /> Final assessment</Link></div><span className="course-privacy"><Check size={14} /> Read the source, complete the lab, pass the chapter check.</span></div><aside className="course-hero-side"><div className="progress-orbit"><strong>{progress}%</strong><span>COURSE<br />PROGRESS</span></div><p>{completed} of {total} lessons complete · {completeChapters} of {course.phases.length} chapters cleared.</p><div className="progress-bar"><span style={{ width: `${progress}%` }} /></div></aside></section><section className="course-layout"><aside className="course-side"><div className="side-label">THE PATH / {course.phases.length} CHAPTERS</div><div className="course-side-list">{course.phases.map((phase) => { const complete = phase.lessons.every((lesson) => done.includes(`${course.id}:${lesson.id}`)); return <a href={`#${phase.id}`} key={phase.id}><span>{complete ? <Check size={13} /> : phase.number}</span>{phase.title}<small>{phase.lessons.length} lessons</small></a>; })}</div><div className="source-box"><span>ORIGINAL SOURCES</span><strong>{course.sourceLabel}</strong><a href={course.sourceUrl} target="_blank" rel="noreferrer">View source map <ArrowUpRight size={14} /></a></div><div className="assessment-side-note"><LockKeyhole size={15} /><span>Certificate unlocks only after every chapter and the server-scored final assessment.</span></div></aside><div className="phase-map">{course.phases.map((phase) => { const complete = phase.lessons.every((lesson) => done.includes(`${course.id}:${lesson.id}`)); return <section className="phase-section" id={phase.id} key={phase.id}><div className="phase-section-head"><div><span className="phase-big-number">{phase.number}</span><div><p className="eyebrow">CHAPTER {phase.number}</p><h2>{phase.title}</h2></div></div><span className={complete ? "phase-project-tag is-complete" : "phase-project-tag"}>{complete ? <Check size={14} /> : <Flag size={14} />} {complete ? "CHAPTER CLEARED" : `LAB / ${phase.project}`}</span></div><p className="phase-description">{phase.description}</p><div className="phase-lessons">{phase.lessons.map((lesson, index) => { const completeLesson = done.includes(`${course.id}:${lesson.id}`); return <Link key={lesson.id} href={`/course/${course.id}/lesson/${lesson.id}`} className={completeLesson ? "lesson-card is-complete" : "lesson-card"}><div className="lesson-card-num">{completeLesson ? <Check size={14} /> : String(index + 1).padStart(2, "0")}</div><div><span className="lesson-card-type">{lesson.video ? "VIDEO + NOTES + LAB" : "SOURCE + NOTES + LAB"}</span><h3>{lesson.title}</h3><p>{lesson.summary}</p></div><div className="lesson-card-end"><span>{lesson.duration}</span>{lesson.video ? <Play size={16} /> : <ArrowUpRight size={16} />}</div></Link>; })}</div>{complete && <div className="chapter-cleared"><Check size={16} /> Chapter complete — the next chapter is ready.</div>}</section>; })}<section className="course-finish-card"><span className="section-index"><Trophy size={14} /> FINISH LINE</span><h2>Ready for the final assessment?</h2><p>Finish every chapter, then take the five-question server-scored test. A passed result makes the informal Rampage certificate request available.</p><Link className="primary-cta" href={`/course/${course.id}/assessment`}>Open final assessment <ArrowUpRight size={15} /></Link></section></div></section></main></Shell>;
}
